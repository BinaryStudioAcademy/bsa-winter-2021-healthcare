{
  "private": true,
  "version": "0.0.1",
  "engines": {
    "node": "12.x.x",
    "npm": "6.x.x"
  },
  "scripts": {
    "build:ts": "tsc && tscpaths -p tsconfig.json --src src --out build",
    "build": "npm run build:ts && cp package.json build"
  },
  "devDependencies": {
    "tscpaths": "0.0.9",
    "typescript": "4.1.2"
  },
  "dependencies": {
    "yup": "0.32.9",
    "yup-phone": "1.2.19"
  }
}
