{
  "private": true,
  "version": "0.0.1",
  "engines": {
    "node": "14.x.x",
    "npm": "6.x.x"
  },
  "scripts": {
    "build:ts": "tsc && tscpaths -p tsconfig.json --src src --out build",
    "build": "npm run build:ts && cp package.json build"
  },
  "devDependencies": {
    "tscpaths": "0.0.9",
    "typescript": "4.1.2"
  },
  "dependencies": {
    "dayjs": "1.10.4",
    "yup": "0.32.9"
  }
}
